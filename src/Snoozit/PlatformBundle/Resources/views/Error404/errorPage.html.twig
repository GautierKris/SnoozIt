{% extends "::base.html.twig" %}

{% block body %}

    <div class="col-lg-12">

        <div class="row">
            {{ render( controller('SnoozitPlatformBundle:Site:breadcrumb', {'breadcrumb': breadcrumb})) }}
        </div>

        <div class="text-center" id="error-box">
            <i class="fa fa-warning fa-5x"></i>
            <h1>Oups, là il y a une erreur...</h1>
            <p>La page demandé n'existe pas désolé</p>

            <h2>Motif : {{ motif }}</h2>
        </div>
    </div>
{% endblock body %}
